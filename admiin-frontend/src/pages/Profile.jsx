// Profile page copied from main frontend for admin UI base
import React from 'react';
import Layout from '../components/Layout';

const Profile = () => (
  <Layout>
    <h1>Admin Profile</h1>
    {/* Add admin profile details here */}
  </Layout>
);

export default Profile;
