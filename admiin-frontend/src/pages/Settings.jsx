// Settings page copied from main frontend for admin UI base
import React from 'react';
import Layout from '../components/Layout';

const Settings = () => (
  <Layout>
    <h1>Admin Settings</h1>
    {/* Add admin settings controls here */}
  </Layout>
);

export default Settings;
